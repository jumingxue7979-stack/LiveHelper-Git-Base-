# LiveHelper 현재 기준

작성일: 2026-05-17

## 결론

앞으로 LiveHelper 작업 기준은 이 폴더 하나입니다.

```text
/Users/opeunkeullo/Desktop/LiveHelper-Git-Base
```

GitHub 기준 저장소는 아래입니다.

```text
https://github.com/jumingxue7979-stack/LiveHelper-Git-Base-
```


## 오늘 최종 인수인계

다음 새창에서는 아래 파일만 먼저 열어 붙여넣으면 됩니다.

```text
NEXT_CHAT_COPY_PASTE_2026-05-17_KO.md
```

오늘 전체 작업 기록은 아래입니다.

```text
docs/final-handoff-2026-05-17-ko.md
```

GitHub/빌드 흐름을 배우는 교육 자료는 아래입니다.

```text
docs/education-guide-git-and-build-2026-05-17-ko.md
```

## 절대 헷갈리지 말 것

- 실제 작업: `LiveHelper-Git-Base`
- 공유 기준: GitHub `main` 브랜치
- Windows에서 받을 때: `git pull`
- Mac에서 작업 후 보낼 때: `git add`, `git commit`, `git push`
- ZIP 수동 전달, 공유 폴더, 날짜별 산출물은 공식 기준이 아닙니다.

## Mac에서 작업 시작

```bash
cd /Users/opeunkeullo/Desktop/LiveHelper-Git-Base
git pull
```

## Windows에서 작업 시작

```powershell
cd C:\Users\A\Documents\Codex\LiveHelper-Git-Base
git pull
```

## Windows EXE 빌드

GitHub 웹에서:

```text
Actions -> Build Windows OBS Live Rank -> Run workflow -> artifact 다운로드
```

Windows 로컬에서:

```powershell
cd C:\Users\A\Documents\Codex\LiveHelper-Git-Base\apps\windows-obs-live-rank
BUILD_ON_WINDOWS.cmd
```

## 보관된 예전 파일

맥 바탕화면의 예전 작업본, 날짜별 ZIP, 인수인계, 스크린샷, 구 LiveRank 프로젝트는 아래로 이동했습니다.

```text
/Users/opeunkeullo/Desktop/_ARCHIVE_DO_NOT_USE_2026-05-17
```

이 archive는 참고용입니다. 앞으로 작업에 사용하지 않습니다.
